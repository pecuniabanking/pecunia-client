{
	Actions = {
		// Define action descriptions here, for example
		"refreshDisplay:" = id;
	};
	Outlets = {
		// Define outlet descriptions here, for example
		delegate = id;
		dataSource = id;
	};
    ClassName = SMPieChartView; 
    SuperClass = NSView; 
}
